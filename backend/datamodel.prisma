type User {
  id: ID! @unique
  name: String!
  email: String!
}

type Bathroom {
  id: ID! @unique
  title: String!
  description: String!
  address: String!
  image: String
  largeImage: String
  # user: User!
}
