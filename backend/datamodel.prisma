type User {
  id: ID! @unique
  name: String!
  email: String!
}

type Bathroom {
  id: ID! @unique
  title: String!
  description: String!
  address: String!
  lat: Float
  lng: Float
  image: String
  largeImage: String
  # user: User!
}
